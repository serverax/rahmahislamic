name: Firebase Deploy

on:
  push:
    branches: [ develop, main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Deploy to Firebase
      run: |
        echo "Firebase deployment configured"
        echo "Project: rahma-app-f7594"
        echo "Ready for Cloud Functions deployment"
